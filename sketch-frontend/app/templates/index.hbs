<div class="box box-max">
  {{#each sketch in sketches}}
    <canvas {{bind-attr id="sketch.url"}} class="sketchpad sketchpad-mini box-inline" data-width="400" data-height="400"></canvas>
  {{/each}}
  {{#if sketches}}
    {{trigger-event this 'setupSketches'}}
  {{/if}}
</div>